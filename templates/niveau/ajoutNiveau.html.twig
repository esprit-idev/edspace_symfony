{% extends 'backOffice.html.twig' %}
{% block title %}<title>Niveaux</title>{% endblock %}
{% block leftNavBar %}
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Niveaux</a>
        </li>
    </ul>
{% endblock %}
{% block cards %}
    <div class="content-wrapper">
    <div class="container-fluid">
    {{ form_start(f,{'attr':{'novalidate':'novalidate'}}) }}
    {{ form_end(f) }}
    <table class="table table-striped">
        <thead>
            <th></th>
            <th></th>
            <th></th>
            <th>Niveau</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </thead>
        <tr {% for n in niveaux %}>
            <td></td>
            <td></td>
            <td></td>
            <td>{{ n.id }} </td>
            <td><a href="{{ path("modifNiveau",{'id':n.id}) }}" class="btn btn-outline-info"><i class="fas fa-pen"></i></a></td>
            <td><a href="{{ path("suppNiveau",{'id':n.id}) }}" class="btn btn-outline-danger"><i class="fas fa-trash"></i></a>
        </tr {% endfor %}>

    </table>
    </div>
    </div>

{% endblock %}