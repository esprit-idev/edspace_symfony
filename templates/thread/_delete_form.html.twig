<form method="post" action="{{ path('thread_display', {'id': thread.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ thread.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
