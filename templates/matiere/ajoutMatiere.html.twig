{% extends "backOffice.html.twig" %}
{% block leftNavBar %}
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Matières</a>
        </li>
    </ul>
{% endblock%}
{% block cards %}
    <div class="content-wrapper">
    <div class="container-fluid">
{{ form(f,{'attr':{'novalidate':'novalidate'}}) }}
<table class="table table-striped">
    <thead>
        <th></th>
        <th></th>
        <th>Nom de la matiere</th>
        <th>Niveau concerné</th>
        <th>Modifier</th>
        <th>Supprimer</th>
    </thead>
    <tr {% for m in matieres %}>
        <td></td>
        <td></td>
        <td>{{ m.id }}</td>
        <td>{{ m.niveau.id }}</td>
        <td><a href="{{ path("modifMatiere",{'id':m.id}) }}" class="btn btn-info">Modifier</a></td>
        <td><a href="{{ path("suppMatiere",{'id':m.id}) }}" class="btn btn-danger">Supprimer</a></td>
    </tr {% endfor %}>
</table>
    </div>
    </div>
    </br>
{% endblock %}